# Copy to .env.local and adjust.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
NEXT_PUBLIC_TRACKING_DEFAULT_COMPANY_ID=1
# Sent as stoppedBy when using Stop tracking (until real login exists).
# NEXT_PUBLIC_ACTING_USER=Clinical Architect
